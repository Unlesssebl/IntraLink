{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local settings surface for the Desktop Companion.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
