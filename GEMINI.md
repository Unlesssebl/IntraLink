# 🧠 Память и контекст проекта Intralink (`GEMINI.md`)

Файл постоянной контекстной памяти для AI-агента Antigravity. Содержит ключевые инварианты инфраструктуры, правила вызова команд и доменную специфику IntraService.

---

## 🏗️ 1. Инфраструктура и окружение (Docker / Базы данных)

* **PostgreSQL (pgvector):**
  * Контейнер: `intralink_postgres`
  * Пользователь: `postgres`
  * База данных: `intraservice`
  * Подключение:
    ```bash
    docker exec intralink_postgres psql -U postgres -d intraservice -c "<SQL>"
    ```
  * Таблица базы знаний RAG: `task_knowledge_base` (векторный индекс HNSW на поле `embedding`, размерность 1024).

* **Redis:**
  * Контейнер: `intralink_redis` (порт в контейнере: 6379, хост: 6380)
  * Токен аутентификации воркера: ключ `worker:service_auth_b64` (зашифрован Fernet, расшифровывается через `decrypt_token`).
  * Кэш каталога услуг: ключ `worker:service_catalog` (TTL 24 часа).
  * Кэш аналитических отчётов нагрузки: ключи `reports:load:month:{year}_{month}` (TTL 30 дней для закрытых месяцев, 5 минут для текущего). Динамический API: `/api/v2/reports/load` и `/api/v2/reports/export`.

* **Core API (Legacy v1 vs Architecture v2):**
  * В ветке `feat/v2-vertical-slice-architecture` проект перешел на модульный монорепозиторий v2 согласно `docs/architecture/v2-architecture-blueprint.md` (ADR 0004).
  * Кодовая база v1 удалена в коммите `9739772`. Проект развивается строго в архитектуре v2: вертикальные срезы `api/src/features/*`, воркер `worker/` и общее ядро `core/`.
  * Для доменной специфики IntraService (раздел 2) и Helpdesk (раздел 4) правила остаются неизменными.

* **🚫 Принцип бескомпромиссного качества (No-Kludge & Root-Cause Policy):**
  * **СТРОГО ЗАПРЕЩЕНО внедрять «костыли», временные заплатки, нетипизированные хаки или маскировать архитектурные дефекты.**
  * **Обязанность агента:** При обнаружении фундаментальной проблемы, несоответствия контрактов, узких мест или концептуальных противоречий в архитектуре — **агент обязан немедленно и открыто обозначить проблему, вскрыть первопричину (root cause) и предложить фундаментальное («big shot») решение**, устраняющее дефект в корне, а не прятать его под слоем условных операторов, фиктивных фолбэков или костылей.

* **🔍 Обязательный аудит прежней реализации (Правило донора v1):**
  * **Если модуль или пакет является объёмным по реализации** или затрагивает интеграцию с внешними системами (IntraService API, Active Directory, сетевые протоколы SMB/WMI, RAG/LLM, аналитика):
  * **СТРОГО ЗАПРЕЩЕНО писать модуль с нуля «вслепую» без предварительного изучения его реализации в прежней версии.**
  * Перед началом кодогенерации обязательно исследовать код и коммиты v1 через `git show 9739772~1:<path>`, `git log -S "<symbol>"` или поиск по ветке.
  * В прежней версии v1 уже были пройдены и решены десятки недокументированных особенностей (edge-cases), нетривиальных форматов ответов и нюансов окружения.
  * Задача при разработке v2 — **не изобретать велосипед с нуля**, а извлечь доменную суть и опыт обработки краевых случаев из v1, упаковав их в строгие контракты (Pydantic v2 DTO), высокую модульность (VSA) и чистый UI/UX.


* **🏛️ Доменная модель, контракты и Zero-Stub Policy:**
  * Полная спецификация терминологии Helpdesk, контрактов и инвариантов зафиксирована в:
    `docs/architecture/domain-model-and-contracts.md`
  * **Zero-Stub Policy:** строгий запрет на создание визуальных заглушек-иллюзий, фиктивных полей данных и неработающих декоративных элементов в интерфейсе. Любое поле и действие в UI обязаны иметь реальный источник данных и соответствовать эталонным Pydantic v2 DTO бэкенда.

* **🤖 Автономный конвейер и система управления автопилотом:**
  * Полная спецификация зафиксирована в: `docs/architecture/v2-automation-pipeline.md`.
  * **Шлюз релевантности (Фильтр №1):** Нецелевые заявки немедленно отменяются (Статус `30` «Отменена») с регламентным комментарием перенаправления. Сценарии решения для них не запускаются.
  * **Рантайм и шина задач:** **Taskiq** (`taskiq-redis` + `taskiq-fastapi` + `taskiq-scheduler`). Linux Docker воркер (`default`/`rag_compute`) + очередь `windows_exec` для внешнего Windows-агента.
  * **Инжестия и пульс:** Двойной срез каждые 30 секунд (фильтр 984 + `ChangedMoreThan`). Двойной watermark: Redis + PostgreSQL `system_state`.
  * **Матрица автономии и предохранитель:** Режимы `FULL_AUTO` / `ASSISTED` / `DISABLED` управляются из UI/БД с кэшем в Redis. При 3 ошибках подряд срабатывает Circuit Breaker с авто-откатом в `ASSISTED`.
  * **Автономный диалоговый цикл:** При нехватке данных или выключенном ПК автопилот сам пишет заявителю инструкцию (Статус `6` «Приостановлена») и автоматически возобновляет работу при получении ответа заявителя (до 2 раундов уточнения).
  * **Прозрачность и аудит:** Все действия автопилота сопровождаются скрытыми техническими заметками (`IsPrivateComment: true`) в истории тикета и отображаются на вкладке «Автопилот» в UI.
  * **Zero Duplicate Logic:** Код сценариев строго един для обоих контуров (Ко-пилот vs Автопилот). Разница лишь в источнике валидации параметров (человек в UI vs системные предикаты).




---

## 🌐 2. Специфика IntraService API

* **Базовый URL:**
  * `settings.INTRASERVICE_URL` уже оканчивается на `/api` (например, `https://servicedesk-pub.corporate.loc/api`).
  * При конструировании относительных путей **не дублировать `/api`**: использовать `/service`, `/task`, `/tasklifetime` и т.д.

* **Особенности формата ответов (`/api/task` и `/api/tasklifetime`):**
  * `GET /api/task/{id}?include=...`: возвращает вложенную структуру `{"Task": {...}, "Statuses": [...], "Priorities": [...], "Attachments": [...]}`. Клиент обязан распаковывать `Task` и подмешивать блоки `include` в целевой DTO.
  * `GET /api/tasklifetime?taskid={id}`: события в `TaskLifetimes` **не содержат полей `Id` и `TaskId`**. Дата события передаётся в ключе `Date` (а не `Created`), автор — в `Editor` (а не `UserName`). DTO обязаны безопасно маппить эти поля и не требовать обязательного `Id`.

* **Типы заявок (`/api/tasktype`):**
  * Запрос детальной информации `GET /api/tasktype/{tasktypeid}` **всегда требует обязательный параметр `serviceid`** (например, `/api/tasktype/4?serviceid=40`), иначе API возвращает `400 Bad Request`.
  * Типы заявок раздела 05 (Directum):
    * Сервисы `40, 41, 233, 234` -> Тип `4` (`Directum`: инциденты, договоры, тендеры, установка; поля `1015, 1016, 1017, 1519`).
    * Сервисы `55, 232` -> Тип `1018` (`Заявка на пользователя DIRECTUM`: онбординг, доступы; поля `1121-1135, 1180, 1488, 1489, 1521`).

* **Ограничения каталога услуг (`/api/service`):**
  * Контроллер сервисов IntraService API работает **строго в режиме чтения (`GET`)**.
  * Запросы `POST` и `PUT` возвращают `405 Method Not Allowed`.
  * Создание, переименование, настройка прав и структуры сервисов производятся **исключительно через веб-интерфейс администратора** (`Настройка` ➔ `Каталог услуг` ➔ `Сервисы`).
  * Единственные доступные POST-методы: `/api/serviceuser` и `/api/servicecompany` (привязка пользователей/компаний к сервисам).

* **Документация:**
  * Полный справочник IntraService API v4.47 находится локально в каталоге:
    `docs/external/intraservice_api/IntraService_API_Index.md`
  * Перед исследованием новых эндпоинтов **всегда сверяться с локальным индексом**.

---

## ⚡ 3. Стандарты выполнения скриптов (Windows / pwsh)

* **Запрет на сложные однострочники через `-c "..."`:**
  * Из-за особенностей парсинга PowerShell вложенные кавычки, обратные слэши, f-строки вида `f'{t.get(\"...\")}'` и регулярные выражения вроде `(\d+)` ломаются шеллом до передачи в Python.
* **Каноничный способ запуска сложных проверок:**
  * Создавать временный скрипт или передавать через конвейер:
    ```powershell
    Get-Content ./scratch_check.py | docker exec -i intralink_api python
    ```

---

## 📚 4. RAG Engine & Семантическая маршрутизация

* **Полнотекстовый поиск и гибридный движок RRF:**
  * Таблица `task_knowledge_base` содержит вычисляемую колонку `search_vector tsvector` (русская морфология, веса: A=название, B=проблема, C=решение) и GIN-индекс `ix_task_kb_search_vector`.
  * Слияние каналов Dense (HNSW pgvector) и Sparse (FTS) выполняется через **Reciprocal Rank Fusion (RRF)**:
    $$RRF(d) = \frac{0.6}{60 + r_{dense}(d)} + \frac{0.4}{60 + r_{sparse}(d)}$$
  * Валидация качества: функция `is_valid_solution_source` отсекает шаблонные отписки («Сделано», «Ок», «Решено по телефону») и короткие неинформативные ответы.

* **Двухуровневый кэш эмбеддингов (`core/rag/embed_cache.py`):**
  * L1 (RAM): Потокобезопасный `InMemoryLRUCache` (`OrderedDict` + `Lock`, емкость 2048 векторов, ~0 мс, <8 МБ RAM).
  * L2 (Redis): Ключ `rag:emb:{model}:{sha256}` (TTL 7 дней, ~1 мс) для синхронизации между процессами API и Worker.
  * Нулевые повторные сетевые вызовы к LiteLLM Gateway для одинаковых текстов.

* **Семантическая маршрутизация без хрупких Regex (`worker/src/scenarios`):**
  * Сценарии декларируют свои естественные образцы фраз в `BaseScenario.semantic_prototypes`.
  * `ScenarioRouter` объединяет прямой эвристический скор Factor A с семантическим сходством Factor E: при высоком семантическом сходстве ($e\_score \ge 0.75$) сценарий распознается даже при опечатках заявителя («забыла парольчик», «не печатает МФУ в 305»).
  * Сценарий `rag_consultation` сохраняет широкий охват в `can_handle()`, выступая надежным fallback-контуром для нетиповых запросов.
  * **Изоляция конкурентных задач:** Запрещено хранить промежуточное состояние решения в полях экземпляра сценария (`_cached_solution`). Решения изолируются строго по `task.id` в `_solutions_by_task[task.id]` с атомарным извлечением через `.pop(task.id)`.

---



