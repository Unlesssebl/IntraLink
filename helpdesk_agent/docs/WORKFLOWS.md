# 📖 Справочник оперативных Workflow и команд Helpdesk Agent

Настоящее руководство описывает все поддерживаемые рабочие процессы (Workflows), диалоговые слэш-команды и текстовые шорткаты AI-ассистента технической поддержки в среде Antigravity (AGY).

---

## 💡 Принцип работы команд и Workflows в Antigravity CLI

> [!NOTE]
> **Как устроены команды в Antigravity (`agy`):**
> 1. **Встроенные системные команды CLI (`/help`, `/exit`, `/goal`, `/schedule`, `/plan`, `/grill-me`, `/learn`, `/teamwork-preview`):**
>    Зашиты в бинарный TUI-интерфейс терминала и отображаются в системном автокомплите по нажатию клавиши `/`.
> 2. **Пользовательские рабочие процессы и навыки (Skills & Workflows):**
>    Определяются через файлы навыков (`.agents/skills/intraservice-helpdesk/SKILL.md`) и системные правила (`helpdesk_agent/GEMINI.md`).
>    Команды вроде `/triage`, `/redirect`, `/task`, `/diag`, `/screen`, `/kb`, `/sync`, `/db` — это **высокоуровневые диалоговые команды агента**. Вы вводите их прямо в чат, и агент мгновенно распознает их, запускает соответствующие I/O-инструменты и выводит результат с интерактивными модальными окнами согласования.

---

## ⚡ Быстрая таблица команд

| Команда | Альтернативные фразы | CLI-вызов | Назначение |
|---|---|---|---|
| `/triage` `[сервис]` `[N]` | `разбери 2 сервис`, `триаж 03`, `пачка 5`, `разбери 1С` | `helpdesk_tool.py batch [--service S] [--limit N]` | Пакетный разбор очереди 1-й линии (Filter 984). Поддерживает фильтрацию по **человеческому номеру сервиса** (`01`..`16`) и лимиту N |
| `/redirect` `[сервис]` `[N]`<br>`/triage [сервис] [N] /redirect` | `отмени редиректы 01`, `заявки на перенаправление`, `триаж 04 /redirect` | `helpdesk_tool.py redirect [--service S] [--limit N]` | Поиск и пакетная отмена заявок, ошибочно зарегистрированных не в том разделе каталога (Статус 30: **Отменена** + маршрутизация) |
| `/services` | `каталог сервисов`, `номера разделов` | `helpdesk_tool.py services` | Список корневых разделов каталога услуг с человеческими номерами (`01`..`16`) для фильтрации в `/triage` |
| `/task <ID>` | `детали <ID>`, `покажи тикет <ID>` | `helpdesk_tool.py task <ID>` | Полная карточка заявки: заявитель, кастомные поля, история, сетевой статус и RAG |
| `/diag <ХОСТ/IP>` | `проверь <ХОСТ>`, `пинг <IP>` | `helpdesk_tool.py diagnose <ХОСТ>` | Экспресс-диагностика хоста (ICMP Ping, DNS, порт SMB:445, WinRM:5985) |
| `/screen <ID>` | `скриншот <ID>`, `покажи вложения <ID>` | `helpdesk_tool.py attachment <ID>` | Скачивание и визуальный анализ скриншотов ошибок из вложений заявки |
| `/kb <запрос>` | `поиск в базе <запрос>`, `как решали <...>` | `helpdesk_tool.py search-kb "<запрос>"` | Семантический RAG-поиск в базе исторических решений (FastEmbed / pgvector) |
| `/sync` `[N]` | `синхронизируй базу`, `обнови знания` | `helpdesk_tool.py sync-kb [--limit N]` | Индексация и автообучение на выполненных (29) и отмененных (30) заявках |
| `/db` | `статус бд`, `проверь базу` | `helpdesk_tool.py check-db` | Проверка состояния контейнера PostgreSQL + pgvector и автозапуск при необходимости |

---

## 🔄 Основные сценарии (Workflows)

### Сценарий 1: Пакетный разбор очереди (`/triage`)

Самый частый ежедневный процесс инженера первой линии. Позволяет разбирать как всю очередь целиком, так и точечно фокусироваться на конкретном разделе каталога (например, только заявки по 1С или только по оргтехнике/принтерам).

```mermaid
graph TD
    A["Ввод /triage, /triage 2 или /triage 03"] --> B["Проверка PostgreSQL (check-db / start-db)"]
    B --> C{"Указан номер сервиса?"}
    C -->|Да| D1["Запрос заявок выбранного раздела (Filter 984 + Service Subtree)"]
    C -->|Нет| D2["Запрос всех заявок очереди (Filter 984)"]
    D1 --> E["Сетевая диагностика хостов заявителей (Ping, SMB, DNS)"]
    D2 --> E
    E --> F["Семантический RAG-поиск по базе решений"]
    F --> G["Вывод компактных 4-строчных карточек"]
    G --> H["Интерактивное модальное окно (ask_question)"]
    H --> I{"Выбор оператора"}
    I -->|"Все / Ок"| J["Применение всех решений (apply)"]
    I -->|"1, 3"| K["Применение выбранных номеров"]
    I -->|"Детали N"| L["Углубленный просмотр карточки /task N"]
    I -->|"Скриншот N"| M["Просмотр вложений /screen N"]
    J --> N["Авто-индексация решений в базу знаний RAG"]
    K --> N
```

#### Примеры вызова:
- `/triage` — разбор всей стопки заявок из фильтра 1-й линии (по умолчанию 5 заявок).
- `/triage 2` (или `/triage 02`, `триаж 2`) — разбор заявок **только по разделу 02** («Установка и настройка программ»).
- `/triage 3` (или `/triage 03`, `триаж оргтехника`) — разбор заявок **только по разделу 03** («Установка и обслуживание оргтехники / Принтеры / ПК»).
- `/triage 6` (или `/triage 06`, `триаж 1с`) — разбор заявок **только по разделу 06** («Вопросы по 1С»).
- `/triage 2 10` — разбор до 10 заявок по разделу 02.

#### Справочник человеческих номеров сервисов:
| Номер | ID | Название раздела | Назначение и типовые заявки в поддереве |
|:---:|:---:|---|---|
| **`01`** | `42` | **01. Учетные записи пользователей** | Создание/изменение УЗ, почта, 1С, Directum, увольнения |
| **`02`** | `18` | **02. Установка и настройка программ** | Активация Office/Windows, установка ПО, сброс кэша |
| **`03`** | `19` | **03. Установка и обслуживание оргтехники** | Компьютеры, ноутбуки, МФУ, принтеры, ремонт, картриджи |
| **`04`** | `20` | **04. Проблемы с сетью и интернетом** | Сетевые кабели, монтаж, Wi-Fi доступ (WORK-NET) |
| **`05`** | `23` | **05. Вопросы по DIRECTUM, B2B** | Directum (договоры, маршруты), B2B торговые площадки |
| **`06`** | `15` | **06. Вопросы по 1С** | 1С:УПП, 1С:ERP, Бухгалтерия, ЗУП, Документооборот |
| **`07`** | `60` | **07. Вопросы по терминалу сбора данных** | Настройка и связь с базой ТСД |
| **`08`** | `72` | **08. Информационная безопасность** | Доступы к папкам/обменникам, USB, PERCO (СКУД), VPN |
| **`09`** | `24` | **09. Электронная цифровая подпись** | ЭЦП для банков (ВТБ, Сбер), СБИС, Госуслуг, КриптоПро |
| **`10`** | `64` | **10. Телефония** | Подключение, переадресация, сбои связи |
| **`11`** | `16` | **11. Общие вопросы** | Прочие инциденты и консультации |
| **`12`** | `125` | **12. Архив КТД** | Централизованное хранилище КД и ТД |
| **`14`** | `136` | **14. MDC системы** | АИС "Диспетчер", DPA |
| **`15`** | `144` | **15. Вопросы по HelpDesk** | Вопросы по самому IntraService / HelpDesk |
| **`16`** | `188` | **16. Вопросы по IPS PDM\PLM** | ПО конструкторских рабочих мест |

---

### Сценарий 1.1: Поиск и отмена заявок на редирект (`/redirect` / `/triage ... /redirect`)

Используется для быстрого выявления и закрытия неверно оформленных заявок (например, пользователь оформил заявку на доступ к папке или ошибку 1С в разделе учетных записей или сети).

```mermaid
graph TD
    A["Ввод /triage 01 5 /redirect или /redirect 04"] --> B["Проверка PostgreSQL (check-db)"]
    B --> C["Выборка заявок из очереди 1-й линии"]
    C --> D["Семантическая классификация текста инцидента"]
    D --> E{"Текущий раздел != Рекомендованный?"}
    E -->|Да| F["Формирование карточки отмены (Статус 30)"]
    E -->|Нет| G["Пропуск корректно оформленных тикетов"]
    F --> H["Презентация карточек с маршрутом редиректа"]
    H --> I["Модальное окно ask_question"]
    I --> J["Применение apply (Статус 30 + маршрут + 5 мин)"]
```

#### Примеры вызова:
- `/triage 01 5 /redirect` (или `/redirect 01 5`) — поиск до 5 заявок в разделе 01, требующих отмены и редиректа.
- `/triage 04 /redirect` (или `/redirect 04`) — поиск заявок на редирект в разделе 04 («Сеть и интернет») с лимитом 5.
- `/triage /redirect` (или `/redirect`) — поиск заявок на редирект по всей очереди 1-й линии.
- `/redirect 01 10` — шорткат прямого вызова (раздел 01, до 10 заявок).

---

### Сценарий 2: Детальный анализ единичной заявки (`/task <ID>`)

Используется для глубокого изучения сложного тикета.

**Пример вызова:**
```text
/task 139022
детали 139022
```

**Что делает агент:**
1. Загружает полные данные по задаче через `helpdesk_tool.py task 139022`.
2. Извлекает распарсенные кастомные поля (ПК заявителя, инвентарный номер, кабинет, телефон).
3. Проверяет доступность ПК в сети.
4. Подтягивает историю переписки и комментариев инженеров.
5. Выполняет RAG-поиск по базе аналогичных проблем и предлагает проект решения.

---

### Сценарий 3: Диагностика рабочей станции (`/diag <ХОСТ>`)

Используется для проверки сетевого состояния ПК пользователя без перехода в сторонние утилиты.

**Пример вызова:**
```text
/diag NTEMW0047
проверь 10.245.9.14
```

**Что делает агент:**
1. Проверяет корректность имени по правилам именования корпоративных хостов (`normalizer.py`).
2. Выполняет DNS-резолвинг в локальном домене.
3. Проверяет ICMP-пинг.
4. Проверяет открытие портов `SMB (445)` и `WinRM (5985)`. Если брандмауэр блокирует ICMP, но SMB открыт — агент сигнализирует, что хост **онлайн**.

---

### Сценарий 4: Анализ вложений и скриншотов (`/screen <ID>`)

Используется, когда заявитель прикрепил скриншот с ошибкой программы или отчета 1С.

**Пример вызова:**
```text
/screen 139063
скриншот 139063
```

**Что делает агент:**
1. Скачивает вложение задачи в локальную директорию через `helpdesk_tool.py attachment <ID>`.
2. Анализирует изображение с помощью встроенного инструмента `view_file`.
3. Описывает текст ошибки на экране, анализирует причину и предлагает готовое решение.

---

### Сценарий 5: Поиск и обучение базы знаний (`/kb` и `/sync`)

**Семантический поиск:**
```text
/kb сбой печати на HP LaserJet в 1С
как решали ошибку заблокированной таблицы 1С?
```

**Синхронизация и автообучение:**
```text
/sync
/sync 100
обнови базу знаний
```

**Что делает агент:**
1. Выгружает закрытые (`29`) и отмененные (`30`) заявки из IntraService.
2. Пропускает их через умный фильтр качества (отсекает пустые отписки вида «сделано», «ок»).
3. Генерирует векторные эмбеддинги (FastEmbed / Gemini Embeddings) и сохраняет в pgvector и локальный кэш.

---

## ⌨️ Шорткаты при интерактивном согласовании

Когда агент вывел карточки стопки заявок, вы можете ответить любым удобным способом:

- **Интерактивный UI:** Отметить чекбоксы в модальном окне `ask_question` и нажать **Submit**.
- **`все` / `+` / `ок`** $\rightarrow$ Согласовать все предложенные действия.
- **`1, 2, 4`** $\rightarrow$ Применить действия только по номерам 1, 2 и 4.
- **`детали 3`** $\rightarrow$ Показать расширенную историю по заявке №3.
- **`скриншот 2`** $\rightarrow$ Скачать и проанализировать скриншот из заявки №2.
- **`шаблон hardware_repair для 1`** $\rightarrow$ Переключить шаблон для заявки №1.
- **`для 2 напиши: <ваш комментарий>`** $\rightarrow$ Отправить кастомный текст в заявку №2.

---

## 📋 Каталог системных статусов IntraService

| ID | Название статуса | Типовое применение |
|---|---|---|
| `27` | **В работе** | Стандартное принятие задачи к исполнению |
| `35` | **Требует уточнения** | ПК не в сети / требуется дополнительная информация от заявителя |
| `48` | **Ожидание устройства** | Аппаратный ремонт / принести системный блок в каб. 112 |
| `30` | **Отменена** | Неверный раздел каталога услуг / дубликат / перенаправление |
| `29` | **Выполнена** | Услуга оказана (Wi-Fi настроен, доступ выдан, инцидент закрыт) |
| `31` | **Открыта** | Первичный статус новой входящей заявки |

---

## 💬 Каталог адаптивных шаблонов (`templates.json`)

Инженер: **Беликов Ален** (ООО «АйТи ТЭМПО», каб. 112, тел. `49-87`).

| Ключ шаблона | Статус | Название | Текст шаблона | Трудозатраты |
|---|:---:|---|---|:---:|
| `wrong_service` | `30` | Неверный раздел каталога | *«Заявка отменена, т. к. создана не в подходящем разделе.\nТребуется оставить заявку в подходящем разделе: {target_service}.\nПо вопросам звоните на номер 49-87.»* | 5 мин |
| `hardware_repair` | `48` | Аппаратный ремонт / Новый ПК | *«Приносите системный блок в АБК 3, 112 каб. на установку операционной системы и настройку ПО.\nПо вопросам звоните на номер 49-87.»* | 15 мин |
| `pc_offline` | `35` | ПК не в сети | *«Не вижу ПК {pc_name} в сети.\n1. Убедитесь в корректности имени ПК;\n2. Перезагрузите компьютер;\n3. Проверьте подключение сетевого кабеля;\n4. Если кабель подключен, проверьте наличие световой индикации в месте подключения кабеля.\nПо вопросам звоните на номер 49-87.»* | 5 мин |
| `1c_issue` | `27` | 1С: Очистка кэша / Настройка | *«Ваша заявка принята в работу. Выполняется проверка информационной базы и очистка кэша 1С на рабочей станции {pc_name}.\nПо вопросам звоните на номер 49-87.»* | 15 мин |
| `printer_issue` | `27` | Принтер / Оргтехника | *«Ваша заявка принята в работу. Выполняется проверка сетевого порта и очереди печати принтера на рабочей станции {pc_name}.\nПо вопросам звоните на номер 49-87.»* | 10 мин |
| `wifi_access` | `29` | Предоставление Wi-Fi | *«Доступ к Wi-Fi предоставлен.\nИспользуйте логин и пароль от вашей учетной записи на ПК. Инструкцию по подключению приложил.\nЕсли возникнут проблемы с подключением, приходите в АБК-3, кабинет 112.»* | 10 мин |
| `in_work_standard` | `27` | Стандартное принятие | *«Ваша заявка принята в работу. По вопросам звоните на номер 49-87.»* | 10 мин |
| `resolved_standard` | `29` | Стандартное завершение | *«Работы по заявке успешно выполнены. Если возникнут вопросы, перезвоните на номер 49-87.»* | 15 мин |

