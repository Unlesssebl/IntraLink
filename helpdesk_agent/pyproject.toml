[project]
name = "helpdesk-agent"
version = "0.1.0"
description = "IntraService Helpdesk Tools for Antigravity AI Agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
